import './index.scss';
import React from 'react';
import { useSelector } from 'react-redux';


const Toolbar = props => {

    return (
        <div>
            toolbar
        </div>
    )
}

export default Toolbar
